{
  "name": "sydney-carpentry-site-preview",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "dev": "wrangler dev --local --ip 127.0.0.1 --port 8787",
    "check": "node scripts/check-site.mjs"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
